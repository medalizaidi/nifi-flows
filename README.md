# NiFi Flow Registry

This repository contains versioned NiFi flows managed by NiFi Registry.

## Structure
- Each bucket is a directory
- Each flow is stored as JSON snapshots

## Managed by
NiFi Registry with Git-based flow persistence
